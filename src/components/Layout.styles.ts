import { createStyles } from '@mantine/core'

export default createStyles(() => ({
  layout: {
    position: 'relative',
    display: 'flex',
    height: '100vh',
    width: '100%',
    flexDirection: 'column',
    overflowX: 'hidden',
  },
}))
